

&star_job

      save_star_job_namelist = .true.
      star_job_namelist_name = 'star_job_to_lgT_9.5.txt'

      load_saved_model = .true.
      saved_model_name = 'after_core_he_burn.mod'

      save_model_when_terminate = .true.
      save_model_filename = 'lgT_9.5.mod'
      required_termination_code_string = 'log_center_temp_limit'

      set_initial_age = .false.
      set_initial_model_number = .false.
      set_initial_number_retries = .false.

      !pgstar_flag = .true.

/ ! end of star_job namelist


&eos
/ ! end of eos namelist

&kap
/ ! end of kap namelist

&controls

      save_controls_namelist = .true.
      controls_namelist_name = 'controls_to_lgT_9.5.txt'

      log_center_temp_limit = 9.50d0

      ! limit max_model_number as part of test_suite
      max_model_number = 3000
      !max_number_retries = 37

! wind

! atmosphere

! rotation

! mlt

! mixing

! timesteps

! mesh

! solver
      
! GYRE output controls
      !write_pulse_data_with_profile = .true.

! output

      num_trace_history_values = 2
      trace_history_value_name(1) = 'rel_E_err'
      trace_history_value_name(2) = 'log_rel_run_E_err'
      
      !photo_interval = 10
      !profile_interval = 1
      !history_interval = 1
      !terminal_interval = 1

/ ! end of controls namelist



&pgstar

!pause = .true.
Profile_Panels3_xmin = 0 ! -101d0
Profile_Panels3_xmax = 2.6 ! -101d0

/ ! end of pgstar namelist
