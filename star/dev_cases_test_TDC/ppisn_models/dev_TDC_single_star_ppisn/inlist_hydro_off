
&star_job

/ !end of star_job namelist


&controls
   use_other_wind = .true.

   use_dPrad_dm_form_of_T_gradient_eqn = .true.
   use_momentum_outer_BC = .true.
   floor_momentum_outer_BC_at_Prad = .true.
   use_split_merge_amr = .false.

   max_mdot_redo_cnt = 200

   ! Avoid immediately repeating a failed startup or post-relax remesh.
   restore_mesh_on_retry = .false.
   num_steps_to_hold_mesh_after_retry = 5
  ! max_num_merge_surface_cells = 5
  ! max_surface_cell_dq = 1d0
  ! min_surface_cell_dq = 1d-8
/ ! end of controls namelist

&pgstar

/ ! end of pgstar namelist
